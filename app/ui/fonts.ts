import { Audiowide, Kanit, PT_Sans_Caption } from "next/font/google";

export const audiowide = Audiowide({
    weight: "400",
    subsets: ["latin"]
});

export const kanit = Kanit({
    weight: "400",
    subsets: ['latin']
});

export const ptSansCaption = PT_Sans_Caption({
    weight: "400",
    subsets: ["latin"]
});

